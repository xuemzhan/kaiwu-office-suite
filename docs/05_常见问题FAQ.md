# V1.4.1 常见问题

## 为什么安装器提示 KexStepup 缺失？

它是 Win7 必装兼容组件。取得经审批的安装包、计算 SHA256 并更新清单后才能继续。

## 为什么 WPS 总结返回 2？

真实总结服务未随包部署。返回 2 表示明确不支持，避免产生假结果。

## 是否完全离线？

安装和本地工具可以离线运行。任何外部模型服务都必须另行配置并经过安全审批；当前套件不默认上传文件。

## Web 页面为什么不能启动程序？

普通浏览器受安全模型限制。页面只展示路径；程序应从开始菜单或资源管理器启动。