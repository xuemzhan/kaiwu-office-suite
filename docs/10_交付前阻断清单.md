# V1.4.1 交付前阻断清单

状态：**未满足，不得正式发布**
更新日期：2026-07-10

## P0 阻断项

| 编号 | 项目 | 当前状态 | 放行条件 |
|---|---|---|---|
| P0-1 | KexStepup-setup.exe | 缺失 | 取得经审批安装包，运行 `scripts/admin/compute_kexstepup_hash.bat` 生成报告，写入 `manifest/SHA256SUMS.txt` 和版本锁定清单，`verify_installers.bat` 通过 |
| P0-2 | Windows 7 SP1 实机安装 | 未执行 | 干净 Win7 SP1 64 位机器完成安装、重启、`check.bat` 无必需项 FAIL |
| P0-3 | AionUI/Hermes/OpenCode | 待验证 | 在 Win7 SP1 实机确认可启动、可卸载、日志正常 |
| P0-4 | WPS/Kaiwu 插件 | 阻断待验证 | 确认 WPS 12.1.0.26375+、插件加载、AI 出站审批、密钥和日志策略均通过 |
| P0-5 | 发布包构建 | 当前被阻断 | `build_zip.bat 1.4.1` 成功生成 ZIP 和 `.sha256`，且包内无日志/报告/临时文件 |

## P1 放行前必须确认

| 编号 | 项目 | 当前状态 | 放行条件 |
|---|---|---|---|
| P1-1 | ppt-workflow | 参考源/门禁 | 若不启用，发布说明保持 unsupported；若启用，必须完成 Python/依赖 Win7 移植和 PPTX 打开测试 |
| P1-2 | XMind | 可选缺失 | 若不交付，文档继续标为可选缺失；若交付，补安装包、SHA256 和验收记录 |
| P1-3 | 内部制品签名/来源 | 待补 | AionUI、Hermes、OpenCode、WPS 插件保留来源审批和恶意代码扫描记录 |
| P1-4 | 回归测试 | 当前 28/28 PASS | 发布前重新运行 `tests/run_tests.ps1` 并归档 `tests/TEST_REPORT.md` |

## 当前允许的结论

- 可以作为 V1.4.1 候选工程继续验收。
- 不得作为正式可安装发布版对外分发。
- 不得宣传 WPS AI 总结、PPT 自动生成/排版、MCP/A2A、端到端加密、自动备份或等保认证为已交付能力。

发布前可先运行 `release_preflight.bat` 进行只读预检；当前预期会因 KexStepup 缺失返回非零。
